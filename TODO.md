# TODOs

[ ] Change navbar_constructor.dart to have a nullable input for an icon as the big button with a callback required but only if the icon is present.
[ ] Change navbar.dart to have the GestureDetector widget imported straight in for the AppBar.title so it can handle straight text or an icon.
  [ ] Edit the actionWidget* Lists depending on whether this is present or not because if it is present, the off-by-one will cause a skip.
